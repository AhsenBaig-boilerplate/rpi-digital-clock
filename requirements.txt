# Python Dependencies for Raspberry Pi Digital Clock
# Compatible with Python 3.9+

# HTTP requests for weather API
requests==2.31.0

# YAML configuration parsing
PyYAML==6.0.1

# Note: pygame installed via system package (python3-pygame) in Dockerfile
# to avoid compilation issues on ARMv6

# Timezone handling
pytz==2023.3

# Type hints runtime support
typing-extensions==4.8.0

# Note: pygame-emojis is optional and not included by default due to build issues on ARMv6
# The application gracefully falls back to ASCII icons if pygame-emojis is unavailable
# To enable emoji support, manually install: pip3 install pygame-emojis>=0.2.0
