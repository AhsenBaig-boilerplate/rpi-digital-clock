# Use balenablocks wifi-connect (newer, better D-Bus compatibility)
FROM balenablocks/wifi-connect:%%BALENA_ARCH%%

# Set custom portal SSID and passphrase
ENV PORTAL_SSID="DigitalClock Setup"
ENV PORTAL_PASSPHRASE="clocksetup"
