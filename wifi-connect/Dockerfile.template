# Use balenablocks wifi-connect with explicit raspberry-pi architecture
# %%BALENA_ARCH%% might resolve incorrectly for RPi Zero
FROM balenablocks/wifi-connect:raspberry-pi

# Set custom portal SSID and passphrase
ENV PORTAL_SSID="DigitalClock Setup"
ENV PORTAL_PASSPHRASE="clocksetup"
